# Usage

Use `paramforge scan TARGET --passive` for URL-only analysis. Active scans use the configured HTTP client, strict host scope, bounded rate, and safe HTML extraction. Review the effective target scope before adding allowlisted domains.

Run `paramforge --help` and command-specific help for all options.
